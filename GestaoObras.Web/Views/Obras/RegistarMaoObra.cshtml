@model GestaoObras.Web.Models.RegistoMaoObra
using System.ComponentModel.DataAnnotations.Schema;

@{
    ViewData["Title"] = "Registar Mão de Obra";
}

<h2 class="mb-3">Registar Mão de Obra</h2>

<div class="card shadow-sm">
    <div class="card-body">
        <form asp-action="RegistarMaoObra" method="post">
            <div asp-validation-summary="All" class="text-danger mb-3"></div>

            <input type="hidden" asp-for="ObraId" />

            <div class="mb-3">
                <label class="form-label">Obra</label>
                <input class="form-control"
                       value="@ViewBag.ObraNome (@ViewBag.ClienteNome)"
                       readonly />
            </div>

            <div class="mb-3">
                <label asp-for="NomePessoa" class="form-label"></label>
                <input asp-for="NomePessoa" class="form-control" />
                <span asp-validation-for="NomePessoa" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="HorasTrabalhadas" class="form-label"></label>
                <input asp-for="HorasTrabalhadas" class="form-control" />
                <span asp-validation-for="HorasTrabalhadas" class="text-danger"></span>
            </div>

            <div class="d-flex justify-content-between">
                <a asp-action="Details" asp-route-id="@Model.ObraId" class="btn btn-outline-secondary">Voltar</a>
                <button type="submit" class="btn btn-primary">Guardar</button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
